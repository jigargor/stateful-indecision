# Kill Switch Rubric (v1 Placeholder)

1. Trigger if events indicate attempts to access data outside the active ecosystem scope.
2. Trigger if repeated malformed ledger writes suggest integrity loss or tool misuse.
3. Trigger if the agent emits self-modification intents that bypass constitution revision protocol.
4. Trigger if external-access requests attempt to bypass alpha-corpus-only constraints.
5. Trigger if operator-specified emergency keywords appear in high-severity error events.
