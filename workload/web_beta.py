from __future__ import annotations

from typing import Never


def search(query: str) -> Never:
    raise NotImplementedError("Beta ecosystem web access is not available in v1")


def fetch(url: str) -> Never:
    raise NotImplementedError("Beta ecosystem web access is not available in v1")
